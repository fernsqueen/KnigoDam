<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="Knigodam.MainPage">

    <StackLayout>
        <Label x:Name="selected" Text="{Binding Source={x:Reference Name=bookList}, Path=SelectedItem.Title}"
            FontSize="Large" />
        <ListView x:Name="bookList"
              HasUnevenRows="True"
              ItemsSource="{Binding Books}"
              ItemTapped="OnItemTapped">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <StackLayout Orientation="Horizontal">
                            <Image Source="{Binding ImagePath}"  WidthRequest="60" HeightRequest="100" />
                            <StackLayout>
                                <Label Text="{Binding Title}" FontSize="20" FontAttributes="Bold"/>
                            </StackLayout>
                        </StackLayout>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackLayout>

</ContentPage>
